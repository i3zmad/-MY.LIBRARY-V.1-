USE MyLibrary;

# My labrary is small, so i have divided it into three shelves on three tables.

CREATE TABLE FirstShelf(
Id INT PRIMARY KEY,
Bname VARCHAR(225),
Pages INT,
Author VARCHAR(225)
);

CREATE TABLE SecondShelf(
id INT PRIMARY KEY,
bname VARCHAR(225),
Pages INT,
Author VARCHAR(225)
);

CREATE TABLE ThirdShelf(
id INT PRIMARY KEY,
bname VARCHAR(225),
Pages INT,
Author VARCHAR(225)
);